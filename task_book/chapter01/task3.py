length = float(input('Enter room length (float format): '))
width = float(input('Enter room width (float format): '))
area = round(length * width, 2)

print(f'Area of room with length {length} and width {width} = {area}')
